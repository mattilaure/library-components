import { StyleSheet } from "react-native"

export default StyleSheet.create({
    homeContainer: {
        flex: 1,
        height: '100%',
        flexDirection: 'column',
        alignItems: 'center'
    },
    inputContainer: {
        flexDirection: 'row'
    }
})